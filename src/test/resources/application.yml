spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:gitusers;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;PASSWORD=;USER=sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

client:
  github:
    url: https://api.github.com/